{
  "name": "jsmodbus",
  "version": "3.1.3",
  "description": "Implementation for the Serial/TCP Modbus protocol.",
  "author": "Stefan Poeter <stefan.poeter@cloud-automation.de>",
  "main": "./dist/modbus.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/Cloud-Automation/node-modbus"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "dependencies": {
    "debug": "^4.1.1"
  },
  "devDependencies": {
    "@types/debug": "^4.1.5",
    "@types/mocha": "^7.0.2",
    "@types/node": "^13.9.5",
    "mocha": "^7.1.1",
    "nyc": "^15.0.0",
    "sinon": "^9.0.1",
    "standard": "^14.3.3",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "test": "npm run build && mocha test/*.test.js",
    "build": "tsc",
    "watch": "npm run build && mocha --watch test/*.test.js",
    "prepublishOnly": "npm run test",
    "prepare": "npm run build",
    "cov": "nyc mocha test/*.test.js"
  },
  "license": "MIT",
  "keywords": [
    "client",
    "server",
    "serial",
    "port",
    "modbus",
    "tcp"
  ],
  "readmeFilename": "README.md",
  "directories": {
    "test": "test",
    "example": "examples"
  },
  "files": [
    "dist/**/*"
  ]
}
